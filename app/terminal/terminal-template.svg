<g class="terminal">
    <rect height="{{height}}" width="15" class="component-terminal nodrag terminal-input" y="{{dy}}" ng-mouseover="mouseOver()" ng-mouseout="mouseOut()"></rect>
    <text class="terminal-label" dy="{{labelDy}}">input</text>
</g>

